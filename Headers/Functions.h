#pragma once

#include <iostream>
#include <string>
#include <vector>

std::vector<std::string> Split(const std::string input, const char splitter);

const int GetLastIndexOf(const std::string input, const char find);

void PrintMainScreen();